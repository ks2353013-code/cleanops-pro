const SECTOR_MULTIPLIER={HOSPITAL:1.45,SCHOOL:1.15,OFFICE:.95,CORPORATE:.95,HOTEL:1.25,FACTORY:1.3,WAREHOUSE:1.1,RETAIL:1,RESIDENTIAL:.9,OTHER:1};
const EMPLOYEE_TIERS=[{min:1,max:25,base:6500,workers:1},{min:26,max:50,base:9500,workers:1},{min:51,max:100,base:14500,workers:2},{min:101,max:200,base:22000,workers:3},{min:201,max:350,base:32000,workers:4},{min:351,max:500,base:45000,workers:5},{min:501,max:750,base:62000,workers:7},{min:751,max:1000,base:82000,workers:9}];
const SERVICE_MULTIPLIER={RECURRING_CLEANING:1,OFFICE_CLEANING:1,HOSPITAL_CLEANING:1.25,SCHOOL_CLEANING:1.05,DEEP_CLEANING:1.35,SANITATION:1.2,FLOOR_CARPET_CARE:1.15,GLASS_CLEANING:1.1,INDUSTRIAL_CLEANING:1.3,POST_CONSTRUCTION:1.5,MOVE_IN_OUT:1.3};
const TERM_DISCOUNT={MONTHLY:1,YEARLY:.92};
function tierForEmployees(value){const employees=Math.max(1,Number(value)||1);return EMPLOYEE_TIERS.find(t=>employees>=t.min&&employees<=t.max)||{min:1001,max:Number.MAX_SAFE_INTEGER,base:82000+Math.ceil((employees-1000)/250)*15000,workers:Math.ceil(employees/110)};}
export function calculateQuote({employees=1,employeeCount,facilityType='OFFICE',sector,serviceType='RECURRING_CLEANING',serviceName,billingTerm='MONTHLY',frequency='DAILY',areaSqFt=0}){const people=Math.max(1,Number(employeeCount??employees)||1);const sectorKey=String(sector||facilityType||'OFFICE').toUpperCase();const serviceKey=String(serviceType||serviceName||'RECURRING_CLEANING').toUpperCase().replace(/[^A-Z0-9]+/g,'_');const term=String(billingTerm).toUpperCase()==='YEARLY'?'YEARLY':'MONTHLY';const tier=tierForEmployees(people);const monthlyPrice=tier.base*(SECTOR_MULTIPLIER[sectorKey]||1)*(SERVICE_MULTIPLIER[serviceKey]||1);const customerPrice=monthlyPrice*TERM_DISCOUNT[term];const annualValue=customerPrice*12;return{pricingModel:'EMPLOYEE_BASED',employees:people,employeeTier:`${tier.min}-${tier.max===Number.MAX_SAFE_INTEGER?'∞':tier.max} employees`,facilityType:sectorKey,serviceType:serviceKey,billingTerm:term,estimatedWorkers:tier.workers,areaSqFt:Number(areaSqFt)||0,areaUsedForPricing:false,monthlyCharge:Number(customerPrice.toFixed(2)),annualCharge:Number(annualValue.toFixed(2)),annualSavings:Number((term==='YEARLY'?monthlyPrice*12-annualValue:0).toFixed(2)),recommendedPrice:Number(customerPrice.toFixed(2)),currency:'INR',frequency};}
