import { transitionJob, checkIn, checkOut } from '../../../../src/lib/work-order';
export async function POST(req){try{const b=await req.json();if(!b.action)return Response.json({error:'action is required'},{status:400});if(b.action==='transition')return Response.json({data:transitionJob(b.job,b.to,b.actor)});if(b.action==='checkin')return Response.json({data:checkIn({job:b.job,workerId:b.workerId,latitude:b.latitude,longitude:b.longitude,at:b.at})});if(b.action==='checkout')return Response.json({data:checkOut({job:b.job,workerId:b.workerId,completed:b.completed,at:b.at})});return Response.json({error:'unsupported action'},{status:400});}catch(e){return Response.json({error:e.message||'Unable to update job'},{status:400});}}
